apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: strimzi-access-operator
  namespace: flux-repos
spec:
  interval: 60m
  url: https://github.com/strimzi/kafka-access-operator
  ref:
    tag: 0.1.0
  ignore: |-
    # exclude all
    /*
    # include charts directory
    !/packaging/helm-charts/helm3/**
